query GetCharacterById($id: ID!) {
	character(id: $id) {
		name
		image
		gender
		status
		created
		species
		origin {
			name
		}
		type
		location {
			id
			name
		}
		episode {
			id
			episode
			name
			air_date
		}
	}
}
